# Web API container — CPU inference via ONNX Runtime (no torch here).
# Pinned exact for a reproducible deployment image. opencv-python-headless
# tracks the desktop opencv-python version (they release in lockstep).
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
python-multipart==0.0.32
aiofiles==24.1.0
numpy==2.3.4
onnxruntime==1.27.0
opencv-python-headless==5.0.0.93
Pillow==12.0.0
