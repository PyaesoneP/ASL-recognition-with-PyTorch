# Render Blueprint
services:
  - type: web
    name: asl-recognition-backend
    runtime: docker
    plan: free
    region: singapore
    envVars:
      - key: MODEL_TYPE
        value: mobilenet_v2
      - key: MODEL_PATH
        value: outputs/models/best_mobilenet_v2.onnx
      - key: PYTHONUNBUFFERED
        value: "1"
